#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <assert.h>
#include "../src/chess_engine.h"
#include <inttypes.h>

int main(int argc, char** argv) {
    float a = 12.2f;
    float b = 12.3f;
    printf("%d\n", (int) (a) / (int) (b));
}
